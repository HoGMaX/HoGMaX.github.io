{
    "scripts": {
      "build:admin": "sass css/admin.scss css/admin.css && sass css/admin.scss css/admin.min.css --style=compressed",
      "build:prompt": "sass css/prompt.scss css/prompt.css && sass css/prompt.scss css/prompt.min.css --style=compressed",
      "build:style": "sass css/style.scss css/style.css && sass css/style.scss css/style.min.css --style=compressed",
      "build:css": "npm run build:admin && npm run build:prompt && npm run build:style"
    },
  "devDependencies": {
    "sass": "^1.104.1"
  }
}
